<Page x:Class="KeMBody.UI.Pages.AddMeasurementsSummaryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:KeMBody.ViewModels;assembly=KeMBody.ViewModels"
      mc:Ignorable="d" Foreground="Transparent">

    <Page.DataContext>
        <vm:SummaryViewModel/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Style="{StaticResource BigTitle}"
            Text="Dziś nie zostały dodane pomiary"/>

        <Button
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0, 50"
            Style="{StaticResource BigButton}"
            Content="Dodaj pomiary"/>
    </Grid>
</Page>
